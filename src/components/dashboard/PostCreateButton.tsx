const PostCreateButton = () => {
    return (
        <button className="absolute right-3 h-[40px] w-[60%] rounded-full border">
            Create a Post
        </button>
    )
}

export default PostCreateButton;